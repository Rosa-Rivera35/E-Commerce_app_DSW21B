<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tiendavirtual_kotlin">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/tienda_icono"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tienda_icono_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiendaVirtualKotlin">
        <activity
            android:name=".Vendedor.MainActivityVendedor"
            android:exported="false" />
        <!-- 🚀 SplashScreenActivity será la primera pantalla que se muestra -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>

                <!-- Acción principal que inicia la app -->
                <action android:name="android.intent.action.MAIN" />
                <!-- Categoría que indica que es el launcher -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 🏠 MainActivity (pantalla principal de la app) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>